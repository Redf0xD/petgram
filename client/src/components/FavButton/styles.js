import styled from 'styled-components'

export const Button = styled.button`
  appearance: none;
  background: transparent;
  border: 0;
  outline: 0;
  padding: 8px;
  display: flex;
  align-items: center;
  & svg {
    margin-right: 4px;
  }
`
